{
  "name": "akk-portal-logic",
  "version": "0.0.0",
  "private": true,
  "description": "Чистая бизнес-логика онбординга АКК (перенос из index.html). Без React/DOM — только функции и данные.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
